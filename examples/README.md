# JazLabs examples

The small, current examples live in [`instrument_clients`](instrument_clients).
Each one assumes that the matching JazLabs server is already running.

The examples that were previously at the top level are preserved in
[`legacy`](legacy).

Equivalent Jupyter notebooks are in `../notebooks/instrument_clients`.
