# Preserved examples

This folder contains the scripts and supporting files that previously lived at
the top of `examples/`. They are preserved for reference while the small,
current client examples live in `../instrument_clients/`.
