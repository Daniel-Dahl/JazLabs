# Phase-calibration figures

Copy selected calibration plots into this directory using the filenames listed
in the parent `README.md`. The image links in that guide will then render as a
complete calibration report.
