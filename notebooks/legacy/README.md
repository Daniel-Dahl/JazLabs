# Preserved notebooks

This folder contains the notebooks and supporting files that previously lived
at the top of `notebooks/`. They are preserved for reference while the small,
current client notebooks live in `../instrument_clients/`.
