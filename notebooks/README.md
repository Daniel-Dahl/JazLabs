# JazLabs notebooks

The small, current instrument examples live in
[`instrument_clients`](instrument_clients). They contain the same operations as
the Python scripts in `../examples/instrument_clients`.

The notebooks and supporting files that were previously at the top level are
preserved in [`legacy`](legacy).
